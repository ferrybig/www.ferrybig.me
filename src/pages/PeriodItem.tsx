import PageWrapper from '../components/PageWrapper';
import PageBase from '../PageBase';

interface Props {
	base: PageBase,
}

export default function PeriodItem({ base }: Props) {
	return (
		<PageWrapper base={base} title="Fernando's Development area" includeWrapper>
			Not implemented yet...
		</PageWrapper>
	);
}
